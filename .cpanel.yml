---
deployment:
  tasks:
    - export DEPLOYPATH=/home/libp7469/public_html
    - /bin/mkdir -p /home/libp7469/deploy_backups && /bin/cp -r $DEPLOYPATH /home/libp7469/deploy_backups/backup_latest 2>/dev/null || true
    - /bin/cp -r dist/* $DEPLOYPATH
    - /bin/cp -f .htaccess $DEPLOYPATH/.htaccess
